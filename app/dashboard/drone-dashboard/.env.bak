# ~/mavsdk_drone_show/app/dashboard/drone-dashboard/.env

# ===========================================
# API CONNECTION CONFIGURATION
# ===========================================

# Choose your deployment mode:
# - "direct" : Direct HTTP connection (for development/internal access)
# - "proxy"  : HTTPS reverse proxy (for production/domain access)  
# - "auto"   : Automatic detection based on domain
REACT_APP_API_MODE=auto

# Direct HTTP Configuration (used when API_MODE=direct or auto-detected)
REACT_APP_SERVER_IP=100.96.32.75
REACT_APP_SERVER_PORT=5000
REACT_APP_FLASK_PORT=5000
DRONE_APP_FLASK_PORT=7070

# Proxy Configuration (used when API_MODE=proxy or auto-detected)
REACT_APP_PROXY_DOMAIN=droneshow.joomtalk.ir
REACT_APP_PROXY_PATH=/api

# Auto-detection domains (comma-separated)
# If accessed via these domains, will use proxy mode automatically
REACT_APP_PROXY_DOMAINS=droneshow.joomtalk.ir,yourdomain.com



# ===========================================
# OTHER SETTINGS
# ===========================================
PORT=3030
REACT_APP_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
GENERATE_SOURCEMAP=false
SKIP_PREFLIGHT_CHECK=true
DANGEROUSLY_DISABLE_HOST_CHECK=true