# VTOL Performance Analyzer v4.1 - Professional Edition
# Python package requirements

# Core dependencies (REQUIRED)
numpy>=1.20.0
matplotlib>=3.5.0
python-dateutil>=2.8.0

# Optional dependencies for advanced export features
reportlab>=3.6.0      # PDF export (falls back to text if not installed)
openpyxl>=3.0.10      # Excel export (falls back to CSV if not installed)
Pillow>=8.0.0         # Image handling for icons and plots

# Future enhancements (not yet required)
# scipy>=1.7.0        # For optimization and sensitivity analysis (Phase E)
# pandas>=1.3.0       # For advanced data analysis features

# Note: tkinter usually comes with Python installation
# If tkinter is not available:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   macOS: brew install python-tk
#   Windows: Reinstall Python with "tcl/tk" option checked

# Verified compatible with:
# - Python 3.7+
# - Windows 10/11
# - macOS 10.14+
# - Ubuntu 20.04+ / Debian 11+
